/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "felipeh6.sg-host.com",
      "fresh-strapi.onrender.com",
      "fresh2.local",
    ],
  },
};

export default nextConfig;
